using System;
using System.IO;
using System.Runtime.Serialization.Json;

namespace JsonToDictionary
{
    class Program
    {
        static void Main(string[] args)
        {
            // Read File
            Action a = () => {
                
            };
            Func<int> b = () => 0;
            // using(var ms = new MemoryStream(Encoding.UTF8.GetBytes(modelJsonString)))
            // {
            //     DataContractJsonSerializer serializer = new DataContractJsonSerializer(typeof());
            //     try
            //     {
            //         var modelFromJsonString = (Model)serializer.ReadObject(ms);
            //     }
            //     catch (Exception e)
            //     {
            //         throw e;
            //     }
            // }
        }
    }
}
